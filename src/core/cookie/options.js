/**
 * cookie 默认配置选项
 *
 * @author: Storm
 * @date: 2018/02/24
 */

// 默认域名
export const domain = location.hostname.replace(/^(\w|\d)+\./, '');

// 默认路径
export const path = '/';

// 默认安全
export const secure = false;